## 1. Layout Structure

- [x] 1.1 Update `src\App.tsx` so the content below the header uses a responsive single-column-to-two-column layout.
- [x] 1.2 Move the existing Control Panel section into the left column and keep Summary, Portfolio Balance, Tax Drag, and Year-by-Year Breakdown in the right column.
- [x] 1.3 Make the desktop sidebar sticky near the top of the viewport without affecting the mobile stacked layout.

## 2. Control Panel Fit

- [x] 2.1 Update `src\components\ControlPanel.tsx` so controls render full-width in the sidebar.
- [x] 2.2 Preserve every existing input, label, hint, range, dropdown option, and re-roll behavior.

## 3. Validation

- [x] 3.1 Confirm the wide-screen layout places inputs left of the output sections.
- [x] 3.2 Confirm the small-screen layout remains stacked with the control panel before outputs.
- [x] 3.3 Run the existing build command to ensure the React/TypeScript app still compiles.
